buildscript {
    repositories {
        mavenCentral()
        maven {
            name = "forge"
            url = "http://files.minecraftforge.net/maven"
        }
        maven {
            name = "sonatype"
            url = "https://oss.sonatype.org/content/repositories/snapshots/"
        }
    }
    dependencies {
        classpath 'net.minecraftforge.gradle:ForgeGradle:1.2-SNAPSHOT'
    }
}

apply plugin: 'java'

compileJava {
	options.encoding = 'UTF-8'
	sourceCompatibility = 1.7
	targetCompatibility = 1.7
}

jar {
	manifest {
		attributes 'FMLAT': 'thaifixes_at.cfg'
		attributes 'FMLCorePlugin': 'com.lion328.thaifixes.coremod.ThaiFixesLoader'
		attributes 'FMLCorePluginContainsFMLMod': 'true'
	}
}

apply plugin: 'forge'

version = "v1.8-2.2-pre2"
group = "com.lion328.thaifixes"
archivesBaseName = "ThaiFixes"

minecraft {
    version = "1.8-11.14.0.1289-1.8"
    runDir = "eclipse"

    mappings = "snapshot_nodoc_20141130"
}

processResources
{
    // this will ensure that this task is redone when the versions change.
    inputs.property "version", project.version
    inputs.property "mcversion", project.minecraft.version

    // replace stuff in mcmod.info, nothing else
    from(sourceSets.main.resources.srcDirs) {
        include 'mcmod.info'
                
        // replace version and mcversion
        expand 'version':project.version, 'mcversion':project.minecraft.version
    }
        
    // copy everything else, thats not the mcmod.info
    from(sourceSets.main.resources.srcDirs) {
        exclude 'mcmod.info'
    }
}
